version: 0.0
os: linux
files:
  - source: /
    destination: /var
hooks:
  ApplicationStart:
    - location: scripts/start_server.sh
      timeout: 300
  ApplicationStop:
    - location: scripts/stop_server.sh
      timeout: 300
